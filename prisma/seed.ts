import {PrismaClient,Role} from '@prisma/client'; import bcrypt from 'bcryptjs'; const db=new PrismaClient(); async function main(){
const categoryNames=['Technology & IT','Professional Services','Research & Data','Business & Administration','Education & Training','Healthcare & Medical'];
for(const name of categoryNames){await db.category.upsert({where:{slug:name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')},update:{},create:{name,slug:name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')}})}
const hash=await bcrypt.hash('ChangeMe12345!',12);const admin=await db.user.upsert({where:{email:'admin@example.com'},update:{},create:{email:'admin@example.com',name:'Platform Admin',passwordHash:hash,role:Role.ADMIN,profile:{create:{skills:['Administration'],categories:['Management']}}}});const owner=await db.user.upsert({where:{email:'demo@example.com'},update:{},create:{email:'demo@example.com',name:'Demo Provider',passwordHash:hash,profile:{create:{skills:['Research','Design'],categories:['Professional Services'],completion:75}}}});await db.project.create({data:{ownerId:owner.id,title:'Build a research and reporting solution',description:'A demonstration project showing the platform workflow from discovery to application.',category:'Professional Services',skills:['Research','Reporting','Data'],location:'Remote',budget:5000}});await db.course.upsert({where:{slug:'platform-fundamentals'},update:{},create:{title:'Platform Fundamentals',slug:'platform-fundamentals',description:'A demonstration learning path.',published:true,modules:{create:[{title:'Getting Started',position:1,lessons:{create:[{title:'Welcome',position:1,content:'Welcome to the learning centre.'},{title:'Profile setup',position:2,content:'Complete your profile for better discovery.'}]}}]}}});console.log({admin:admin.email,owner:owner.email,password:'ChangeMe12345!'})}main().finally(()=>db.$disconnect());
