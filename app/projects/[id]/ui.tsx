'use client'; import {useState} from 'react'; export function ApplyButton({projectId}:{projectId:string}){const [done,setDone]=useState(false),[p,setP]=useState('');async function go(){const r=await fetch('/api/applications',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({projectId,proposal:p})});if(r.ok)setDone(true);else alert((await r.json()).error)}if(done)return <span className="badge">Application submitted</span>;return <div className="grid"><textarea className="input" placeholder="Write your proposal" value={p} onChange={e=>setP(e.target.value)}/><button className="btn primary" onClick={go}>Submit application</button></div>}
