import {NextResponse} from 'next/server'; import {z} from 'zod'; import {db} from '@/lib/db'; import {hashPassword,setAuth} from '@/lib/auth';
const schema=z.object({name:z.string().min(2).max(100),email:z.string().email(),password:z.string().min(10).max(100)});
export async function POST(req:Request){try{const b=schema.parse(await req.json());if(await db.user.findUnique({where:{email:b.email.toLowerCase()}}))return NextResponse.json({error:'Email already registered'},{status:409});const user=await db.user.create({data:{name:b.name,email:b.email.toLowerCase(),passwordHash:await hashPassword(b.password),profile:{create:{skills:[],categories:[]}}}});await setAuth(user.id);await db.auditLog.create({data:{actorId:user.id,action:'REGISTER',entityType:'User',entityId:user.id}});return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e?.issues?.[0]?.message||'Invalid request'},{status:400})}}
