'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[e,setE]=useState('');const r=useRouter();async function go(x:any){x.preventDefault();const z=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const d=await z.json();if(!z.ok)return setE(d.error);r.push('/dashboard')}return <main className="container" style={{maxWidth:520,paddingTop:60}}><div className="card"><h1>Welcome back</h1><form onSubmit={go} className="grid" style={{marginTop:20}}><input className="input" placeholder="Email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/>{e&&<p style={{color:'#b91c1c'}}>{e}</p>}<button className="btn primary">Sign in</button></form></div></main>}
