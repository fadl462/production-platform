import {getUser} from '@/lib/auth'; import {db} from '@/lib/db'; import {redirect} from 'next/navigation'; import Link from 'next/link';
export default async function Dashboard(){const u=await getUser();if(!u)redirect('/login');const [projects,apps,notes]=await Promise.all([db.project.count({where:{ownerId:u.id}}),db.application.count({where:{applicantId:u.id}}),db.notification.count({where:{userId:u.id,readAt:null}})]);return <main className="container"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><span className="badge">{u.verificationStatus}</span><h1>Good to see you, {u.name}</h1><p className="muted">Your command centre for opportunities, projects and learning.</p></div><Link href="/projects" className="btn primary">Discover projects</Link></div><div className="grid" style={{gridTemplateColumns:'repeat(3,1fr)',marginTop:25}}>{[['Your projects',projects],['Applications',apps],['Unread notifications',notes]].map(([a,b])=><div className="card" key={a as string}><div className="muted">{a}</div><div className="stat">{b}</div></div>)}</div><div className="grid" style={{gridTemplateColumns:'2fr 1fr',marginTop:25}}><div className="card"><h2>Profile readiness</h2><div style={{height:10,background:'#e5e7eb',borderRadius:9,margin:'18px 0'}}><div style={{height:10,width:`${u.profile?.completion||0}%`,background:'#2563eb',borderRadius:9}}/></div><p className="muted">Complete your profile to improve discovery and matching.</p></div><div className="card"><h2>Quick actions</h2><p><Link href="/projects">Browse projects →</Link></p><p>Update profile →</p><p>View messages →</p></div></div></main>}
