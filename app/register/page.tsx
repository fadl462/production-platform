'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Register(){const [f,setF]=useState({name:'',email:'',password:''});const [e,setE]=useState('');const r=useRouter();async function submit(ev:any){ev.preventDefault();const res=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});const d=await res.json();if(!res.ok)return setE(d.error);r.push('/dashboard')}return <main className="container" style={{maxWidth:520,paddingTop:60}}><div className="card"><h1>Create account</h1><p className="muted">Start building your profile and discovering opportunities.</p><form onSubmit={submit} className="grid" style={{marginTop:20}}>{['name','email','password'].map(k=><input key={k} className="input" type={k==='password'?'password':k==='email'?'email':'text'} placeholder={k[0].toUpperCase()+k.slice(1)} value={(f as any)[k]} onChange={x=>setF({...f,[k]:x.target.value})} required/>)}{e&&<p style={{color:'#b91c1c'}}>{e}</p>}<button className="btn primary">Create account</button></form></div></main>}
