import {db} from '@/lib/db'; import {notFound} from 'next/navigation'; export default async function Course({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=await db.course.findUnique({where:{slug},include:{modules:{include:{lessons:true},orderBy:{position:'asc'}}}});if(!c)notFound();return <main className="container" style={{maxWidth:900}}><div className="card"><span className="badge">Learning</span><h1>{c.title}</h1><p className="muted">{c.description}</p>{c.modules.map(m=><section key={m.id} style={{marginTop:25}}><h2>{m.position}. {m.title}</h2>{m.lessons.map(l=><div key={l.id} style={{padding:'12px 0',borderBottom:'1px solid #eee'}}>{l.position}. {l.title}</div>)}</section>)}</div></main>}
